# Skill: Create PR

1. Review `git status` and `git diff`.
2. Run formatter, static checks, and tests defined by the project.
3. Commit only intended files with `<type>(<scope>): <summary>`.
4. Prepare PR body with summary, rationale, and tests.
5. Mention docs or migration impacts when applicable.
